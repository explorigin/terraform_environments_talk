# Deploying Terraform to Multiple Environments

This presentation is built with [reveal.js](https://revealjs.com/).

Run the local server to display slides:

```sh
npm install
npm start
```

And open your browser to [http://localhost:8000](http://localhost:8000).
